# Configuration file paths for the Web GUI backend
# Copy this file to .env and set the actual paths

# Required:
VLM_CONFIG_PATH=./config/vlm_raw.ini
VLM_REVIEW_CONFIG_PATH=./config/vlm_review.ini
NER1_CONFIG_PATH=./config/llm_cat_qwen2.5-72b.ini
NER2_CONFIG_PATH=./config/llm_cat_llama-3.3.ini
DB_CONFIG_PATH=./db_config.ini
UPLOAD_FOLDER=./uploads # Path for uploaded files (relative to where app is run)

# Optional (can override defaults in web_app.py if needed)
# DEFAULT_OUTPUT_DIR=./output_comparison
# DEFAULT_TEMP_DIR= # Leave empty to use system default